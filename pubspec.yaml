name: mfu_fixflow
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
    
  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.12.0
  google_fonts: ^7.0.2
  image_picker: ^1.2.1
  shared_preferences: ^2.5.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/

# ✅ การตั้งค่าไอคอนแอป
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/mfu-fixflow.png"
  min_sdk_android: 21